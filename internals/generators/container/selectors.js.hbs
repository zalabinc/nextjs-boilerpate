import { createSelector } from 'reselect';
// import isTruthy from 'helpers/isTruthy';
// import isFalsy from 'helpers/isFalsy';
// import isImmutable from 'helpers/isImmutable';

const select{{ properCase name }} = (state) => state.get('{{ camelCase name }}');

const makeSelect{{ properCase name }} = () => createSelector(
  select{{ properCase name }},
  (substate) => substate.toJS()
);

export default makeSelect{{ properCase name }};
export {
  select{{ properCase name }},
};
